require 'vizkit'
viz = Vizkit.default_loader.OctomapWrapperVisualization

# create an instance of OctomapWrapper

# a global index
i = 0.0
# initialize timer
timer = Qt::Timer.new
timer.connect(SIGNAL('timeout()')) do


end

# start the timer with a timeout of 10 ms
timer.start(10)
Vizkit.exec
